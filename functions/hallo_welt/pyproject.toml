[project]
name = "hallo-welt-lambda"
version = "0.1.0"
description = "Lambda Funktion für Hallo Welt"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
